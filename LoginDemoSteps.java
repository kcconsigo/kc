package StepDefinitions;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginDemoSteps {

	/*
	 * WebDriver driver = null;
	 * 
	 * @Given("browser is launch") public void browser_is_launch() {
	 * System.out.println("Display the following");
	 * 
	 * System.setProperty("webdriver.chrome.driver",
	 * "C:/Users/KC/eclipse-workspace/CucumberJava/src/test/resources/drivers/chromedriver.exe"
	 * );
	 * 
	 * driver = new ChromeDriver();
	 * 
	 * driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);
	 * driver.manage().timeouts().pageLoadTimeout(40, TimeUnit.SECONDS);
	 * 
	 * driver.manage().window().minimize(); }
	 * 
	 * @And("user is on login page") public void user_is_on_login_page() {
	 * System.out.println("Display the following");
	 * 
	 * driver.get("https://example.testproject.io/web/"); }
	 * 
	 * @When("^user enters (.*) and (.*)$") public void
	 * user_enters_username_and_password(String username, String password) {
	 * System.out.println("Display the following");
	 * 
	 * driver.findElement(By.id("name")).sendKeys(username);
	 * driver.findElement(By.id("password")).sendKeys(password); }
	 * 
	 * @And("user invoks login button") public void user_invoks_login_button() {
	 * System.out.println("Display the following");
	 * 
	 * driver.findElement(By.id("login")).click();
	 * 
	 * driver.findElement(By.xpath("//button[@id='login']")).sendKeys(Keys.ENTER);
	 * 
	 * }
	 * 
	 * @Then("user is navigated to the home page") public void
	 * user_is_navigated_to_the_home_page() throws InterruptedException {
	 * System.out.println("Display the following");
	 * 
	 * Thread.sleep(3000);
	 * 
	 * driver.getPageSource().contains("TestProject Example page"); driver.close();
	 * driver.quit(); }
	 */
}
